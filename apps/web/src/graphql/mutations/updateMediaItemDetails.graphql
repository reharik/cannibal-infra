mutation UpdateMediaItemDetails($input: UpdateMediaItemDetailsInput!) {
  updateMediaItemDetails(input: $input) {
    data {
      mediaItemId
      title
      description
      takenAt
    }
    errors {
      code
      message
      field
      category
      retryable
    }
  }
}
