/**
 * Shared domain types used by multiple entities.
 */

import type { ResourceTypeEnum } from "@photo-app/contracts";

/** Resource that can be shared via ShareLink or commented on. */
export type ResourceKind = ResourceTypeEnum;
