# Infrastructure layer

Persistence, storage, and external integrations live here.

- Repository implementations (e.g. Knex-based `AlbumRepository`)
- HTTP transport (routes, controllers) when added
- External service clients
- No business logic; only technical implementation of interfaces defined in domain/application
