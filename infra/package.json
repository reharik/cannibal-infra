{
  "name": "cannibal-infra",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "npm run lint:shell && npm run lint:yaml",
    "lint:shell": "shellcheck $(git ls-files '*.sh')",
    "lint:yaml": "yamllint ."
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "yaml": "^2.3.4"
  }
}
